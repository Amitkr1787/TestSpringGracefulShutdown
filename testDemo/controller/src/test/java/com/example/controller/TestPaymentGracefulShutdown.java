import org.junit.jupiter.api.Test;\nimport org.springframework.beans.factory.annotation.Autowired;\nimport org.springframework.boot.test.context.SpringBootTest;\nimport org.springframework.boot.test.web.client.TestRestTemplate;\nimport org.springframework.http.ResponseEntity;\nimport org.springframework.web.bind.annotation.RestController;\n\n@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)\npublic class TestPaymentGracefulShutdown {\n\n    @Autowired\n    private TestRestTemplate restTemplate;\n\n    @Test\n    public void testPaymentAndShutdown() throws InterruptedException {\n        // Call the processPayment method\n        ResponseEntity<String> paymentResponse = restTemplate.postForEntity("/processPayment", null, String.class);\n        System.out.println("Payment response: " + paymentResponse.getBody());\n\n        // Wait for 30 seconds before calling shutdownApp\n        Thread.sleep(30000);\n\n        // Call the shutdownApp method\n        ResponseEntity<String> shutdownResponse = restTemplate.postForEntity("/shutdownApp", null, String.class);\n        System.out.println("Shutdown response: " + shutdownResponse.getBody());\n    }\n}